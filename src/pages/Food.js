import React from "react";
import { Box, Typography } from "@mui/material";

const Food = () => {
  return (
    <Box sx={{ padding: "2rem", textAlign: "center" }}>
      <Typography variant="h4" color="#35CEA0">
        Welcome to the Food Page!
      </Typography>
    </Box>
  );
};

export default Food;
